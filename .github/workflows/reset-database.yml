name: Reset database to clean SuperAdmin baseline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show reset instructions
        run: |
          echo "Database reset workflow restored."
          echo "Configure Firebase Admin credentials as GitHub Actions secrets before enabling destructive reset steps."
          echo "This workflow intentionally does not contain production credentials."
